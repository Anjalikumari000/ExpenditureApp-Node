<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <%- include("../partials/navbar") %> 
    <% if(locals.errorMessage !=null) { %>
        <div class="max-w-lg mt-10 px-6 py-3 bg-red-200 border border-red-600 rounded-md mx-auto">  
            <h2 class = "text-red-800 text-center"><%= errorMessage %></h2>
        </div> 
    <% } %>

    <% if(locals.successMessage !=null) { %>
        <div class="max-w-lg mt-10 px-6 py-3 bg-teal-200 border border-teal-600 rounded-md mx-auto">  
            <h2 class = "text-teal-800 text-center"><%= successMessage %></h2>
        </div> 
    <% } %>
    
    
    <%- body %> 
</body>
</html>